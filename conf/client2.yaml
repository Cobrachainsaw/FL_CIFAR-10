workdir: ./storage
mode: client

node:
  name: Client2
  healthcheck: 120.0
  heartbeat: 10.0

  token_projects_initial:
    - name: california_clients
      token: 58981bcbab77ef4b8e01207134c38873e0936a9ab88cd76b243a2e2c85390b94

join:
  first: False
  url: http://localhost:1456

database:
  username: ${DATABASE_USER?Missing DATABASE_USER for client}
  password: ${DATABASE_PASS?Missing DATABASE_PASS for client}
  scheme: ${DATABASE_SCHEMA?Missing DATABASE_SCHEMA for client}
  memory: False
  dialect: postgres # Changed to match the docker-compose database
  host: flotta_database   # Point to the database container defined in docker-compose

datasources:
  - name: CIFAR-10
    type: csv
    path: "/flotta/data"  # Updated to match the mounted volume inside the container
    kind: file
    token: 58981bcbab77ef4b8e01207134c38873e0936a9ab88cd76b243a2e2c85390b94
